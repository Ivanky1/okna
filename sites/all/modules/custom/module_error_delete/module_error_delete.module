<?php

function module_error_delete_update_7100() {
    $modules = array(
        'flowplayer',
    );
    db_delete('system')
        ->condition('name', $modules, 'IN')
        ->condition('type', 'module')
        ->execute();
}
